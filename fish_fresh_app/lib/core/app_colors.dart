import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand — deep Zambian teal
  static const primary = Color(0xFF0A7B5C);
  static const primaryLight = Color(0xFF13A87D);
  static const primaryDark = Color(0xFF065C44);
  static const primarySurface = Color(0xFFE8F5F0);

  // Accent — warm amber (Zambian sunset)
  static const accent = Color(0xFFE8A020);
  static const accentLight = Color(0xFFFFF3DC);

  // Freshness levels
  static const fresh = Color(0xFF1BAD7A);
  static const freshSurface = Color(0xFFE3F8F0);
  static const acceptable = Color(0xFFD4A017);
  static const acceptableSurface = Color(0xFFFFF8E1);
  static const poor = Color(0xFFE07020);
  static const poorSurface = Color(0xFFFFF0E6);
  static const spoiled = Color(0xFFD63B3B);
  static const spoiledSurface = Color(0xFFFFECEC);

  // Neutrals — light mode
  static const white = Color(0xFFFFFFFF);
  static const background = Color(0xFFF5F8F7);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFF0F5F3);
  static const border = Color(0xFFE0EAE6);
  static const borderLight = Color(0xFFF0F5F3);

  // Text — light mode
  static const textPrimary = Color(0xFF0D1F1A);
  static const textSecondary = Color(0xFF4A6B60);
  static const textTertiary = Color(0xFF8BA89F);
  static const textHint = Color(0xFFB0C8C0);

  // Neutrals — dark mode
  static const darkBackground = Color(0xFF0A1410);
  static const darkSurface = Color(0xFF111E19);
  static const darkSurfaceVariant = Color(0xFF182820);
  static const darkBorder = Color(0xFF1E3028);
  static const darkTextPrimary = Color(0xFFE8F2EF);
  static const darkTextSecondary = Color(0xFF8BBFB0);
  static const darkTextTertiary = Color(0xFF4A7A6A);
}
